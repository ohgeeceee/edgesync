interface:
  display_name: "Edgesync"
  short_description: "Repo-specific patterns and workflows for edgesync"
  default_prompt: "Use the edgesync repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true